// Moved these into their own file because the ball needs the screen dimensions
// for collision detection

#ifndef CONSTANTS_H
#define CONSTANTS_H

constexpr int SCREEN_WIDTH = 1600 / 2;
constexpr int SCREEN_HEIGHT = 900 / 2;
constexpr int FPS = 120;

#endif // CONSTANTS_H